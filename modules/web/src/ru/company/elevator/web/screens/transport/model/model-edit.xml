<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://schemas.haulmont.com/cuba/screen/window.xsd"
        caption="msg://modelEdit.caption"
        focusComponent="form"
        messagesPack="ru.company.elevator.web.screens.transport.model">
    <data>
        <instance id="modelDc"
                  class="ru.company.elevator.entity.transport.Model">
            <view extends="_local"/>
            <loader/>
        </instance>
    </data>
    <dialogMode height="AUTO"
                width="AUTO"
                modal="true"
                forceDialog="true"/>
    <layout expand="editActions" spacing="true">
        <form id="form" dataContainer="modelDc">
            <column width="350px">
                <textArea id="nameField" property="name" rows="3"/>
                <textArea id="descriptionField" property="description" rows="5"/>
            </column>
        </form>
        <hbox id="editActions" spacing="true">
            <button id="commitAndCloseBtn" action="windowCommitAndClose"/>
            <button id="closeBtn" action="windowClose"/>
        </hbox>
    </layout>
</window>
